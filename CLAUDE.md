# このリポジトリについて

「月は見ていなくても存在するか」というアインシュタインの疑問から出発し、ベルの不等式（ウィグナー型）を集合の数え上げで導出して、量子力学がそれを破ることの確認、実験史、量子暗号への応用までを全8ステージでたどる学習クエストサイト。
公開URL（予定）: https://ruglerugle.github.io/bell-inequality/

**このサイトの主役は「不等式がどうやって導かれ、なぜ破れるのか」**。天下りに結論を並べるのではなく、
局所実在論を「説明書モデル」に翻訳し、個数の数え上げだけで不等式を組み、sin²(角度差) と衝突させる過程を自分の手で追体験できることを最優先する。

# コミュニケーション
- 日本語で応答する（コード・変数名は英語）
- 簡潔に回答し、自明な説明は省略する
- 複雑なタスクでは実装前に計画を提示し、承認後に着手する

# Git規約
- Conventional Commits形式、本文は日本語（例: `feat: ユーザー認証にOAuth2を追加`）
- 確認なしに自動コミット・自動pushしない
- pushするとGitHub Pagesに即反映される。ビルドが走らないことがあるので、
  反映されない場合は `gh api -X POST repos/ruglerugle/bell-inequality/pages/builds` で手動トリガー

# 対象読者（最重要）

**大学レベルの内容を、優秀な中学生にもわかるように解説する**のが目標。説明不足が最大のNG。

- 専門用語（偏光・相関・隠れた変数・実在性・局所性・sin/cosなど）は初出時に必ず中学生向けの言葉で定義する
- 「AだからB」の各ステップを飛ばさない。1つの結論に複数ターンの問答をかけてよい
- 式は導出の途中経過まで書き、記号の読み下しを添える（D(A,B)＝角度A,Bで測ったとき結果が食い違う割合、など）
- 日常のアナロジーを積極的に使う（握ったコイン、手袋のペア、説明書、裁判・法廷、柵とロープの波など）
- 「高校で習った」のような、中学生を除外する前提の言い回しを避ける
- **分量が増えることをためらわない。簡潔さより丁寧さを優先する**

# 内容上の約束（物理）

- 不等式は**ウィグナー型** D(A,C) ≤ D(A,B)＋D(B,C)（A=0°, B=30°, C=60°、Dは食い違い率）を主役にする。CHSHはSTAGE7で「いとこ」として紹介のみ
- もつれペアの相関の約束は「**同じ角度なら必ず同じ結果**」（Φ+型）。全ステージでこの向きに統一する
- 量子力学の予言は D＝sin²(角度差)。アリス○→ボブの光子が角度aの偏光になる、という教科書的な collapse 描像で導出している
- 「破れ＝局所実在論の否定」であって量子力学の証明ではない、非通信定理で相対論とは矛盾しない、という限定を崩さない

# キャラクターのセリフ

**うさ美 ＝「優秀な中学生」役**
- **知識は中学生レベルまで**。高校以上の事実（sin/cos・偏光・相対性理論の内容等）を
  「〜でしたね」と既知のように語らせない。それらは必ずねこ博士が教える
- **思考力はずば抜けている**。教わった直後の材料から帰結を導く・矛盾を突く・鋭い質問をする・
  アナロジーを自作する、が彼女の見せ場。「ということは……」で先回りして正しく推論するのは大いに可
- 卑屈・無知アピールはキャラ崩壊。分からないことは堂々と質問する
- 例外: 量子力学の基礎（quantum-quest：二重スリット・光子・測定の確率）は学習済みという設定。
  ただし復習として自分で整理し直す形にし、それらを見ていない読者にも完結した説明にする

**ねこ博士**
- 柔らかい先生口調（「〜ね」「〜なんだ」「〜だよ」「〜かな？」「〜してごらん」）
- 相手に同意を迫る「〜だろう？」「〜じゃないか？」は使わない（自問の「〜だろう？」は可）
- 用語・事実・歴史はすべて博士が供給する

**共通**
- 授業型なので「次のステージで」等のメタ言及は許容（物語型ゲームでは禁止）
- セリフを書いたら (1)「だろう？」「じゃないか？」をgrep、
  (2) うさ美の全セリフについて「この知識は中学生が持っているか？ 直前に博士が教えたことからの推論か？」を確認
- **回りくどい前置きを入れない**。疑問は端的にぶつけさせる
- 会話の中で引用（カギ括弧）を使うときは、参照先が直前にあることを確認する

# 表記ルール
- 累乗は `^` ではなく上付きまたは記号（`sin²θ`、`cos²θ`）で書く
- 測定結果は「○（通過）／×（吸収）」で統一
- 角度は「A＝0°、B＝30°、C＝60°」、角度差は「Δ」で統一

# ファイル構成
- `design-system.css` — quest-template由来の共通デザイン。配色は `:root` の変数で管理（夜空の藍×銀）
- `js/progress.js` — 進捗管理・クイズ判定。**クイズの正解は `ANSWERS`**。localStorageキーは `bellQuestProgress`、グローバルは `BQ`
- `stage1.html` 〜 `stage8.html` — 各ステージ。`index.html`（表紙）、`complete.html`（クリア画面）
- `images/` — キャラ画像はWebP（幅300px程度、schrodinger-equationから流用）。`<img>` には `width`/`height` 属性を必ず付ける。
  favicon.png は schrodinger-equation 由来（流用。差し替え検討）。ogp.png は未作成
- 図はSVGで手書きし、JSでアニメーションさせる場合は `prefers-reduced-motion` で静止画にフォールバックする

# 禁止事項
- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない
